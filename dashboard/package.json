{
  "name": "cpf-dashboard-server",
  "version": "1.0.0",
  "description": "CPF Dashboard Server - Unified server for SOC Dashboard, Auditing Dashboard, and Field Kit Client",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "generate:demo": "node scripts/generate_demo_organizations.js",
    "db:schema": "psql cpf_db -f postgres/db_schema.sql",
    "db:seed": "node postgres/db_seed_demo.js",
    "db:setup": "createdb cpf_db && npm run db:schema && npm run db:seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cpf",
    "dashboard",
    "bayesian",
    "security",
    "field-kit"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "pdfkit": "^0.17.2",
    "pg": "^8.11.3"
  }
}
