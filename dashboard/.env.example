# ====================================================================
# CPF DASHBOARD SERVER CONFIGURATION
# ====================================================================
#
# Istruzioni:
# 1. Rinomina questo file in .env
# 2. Decommenta e imposta le variabili che ti servono.
#
# DATABASE_TYPE: Scegli il sistema di storage.
#   - "json": (Default) Usa la struttura originale basata su file JSON. Nessun'altra configurazione richiesta.
#   - "sqlite": Usa un singolo file di database SQLite. Leggero e ideale per lo sviluppo locale.
#   - "postgres": Usa un server PostgreSQL. Richiede la stringa di connessione DATABASE_URL.
#
DATABASE_TYPE=postgres

# ====================================================================
# CONFIGURAZIONE POSTGRESQL
# ====================================================================
#
# Usato solo se DATABASE_TYPE="postgres".
# Formato: postgresql://[USER]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]
#
# Esempio per un'installazione locale standard di PostgreSQL:
DATABASE_URL="postgres://dashboard_user:[INSERT_PASSWORD]@localhost:5432/cpf_dashboard_db"

# ====================================================================
# AUDITOR FIELD KIT PATH (Opzionale)
# ====================================================================
#
# Path assoluto alla cartella 'auditor field kit' contenente i file
# JSON degli indicatori interattivi.
#
# Se non specificato, il server rileva automaticamente il path:
# - Prova: ../auditor field kit (dalla cartella dashboard/)
# - Prova: ../../auditor field kit (fallback)
#
# Imposta questa variabile solo se:
# - Stai deployando su una piattaforma cloud (Koyeb, Heroku, etc.)
# - La struttura delle cartelle del progetto è diversa dallo standard
# - Vuoi forzare un path specifico
#
# Esempi:
# AUDITOR_KIT_PATH=/workspace/auditor field kit        # Koyeb
# AUDITOR_KIT_PATH=/app/auditor field kit              # Docker/Heroku
# AUDITOR_KIT_PATH=/custom/path/auditor field kit      # Custom
#
# AUDITOR_KIT_PATH=
